import 'package:flutter/material.dart';

class Reelpage extends StatelessWidget {
  const Reelpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is a reels page"),
    );
  }
}
